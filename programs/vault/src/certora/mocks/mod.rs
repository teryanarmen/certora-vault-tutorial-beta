// Mocks that simplify functionality for verification
// 
// Add modules in the same hierarchy as in the original code
// For example, a function `my_mod::fun1` is mocked by
// `certora::mocks::my_mod::fun1`

pub mod processor;